# 매쓰홀릭 마케팅 활용 및 초상권 사용 동의서 — 웹폼 전환 기획안

## 1. 요약

원본 워드 동의서를 온라인 신청/동의 폼으로 전환하고, 하단에 **전자서명**을 받아, 제출 즉시 **구글 스프레드시트**에 자동으로 적재되는 구조를 제안합니다.

- 별도 서버·DB 없이 **정적 웹페이지 + Google Apps Script(GAS)** 조합으로 구현 가능 (무료, 유지보수 최소)
- 서명은 마우스/터치로 그리는 **캔버스 서명패드** 방식
- 제출 데이터는 실시간으로 시트에 한 행씩 쌓임

---

## 2. 페이지 구성 (원문 내용 매핑)

원문은 크게 4개 블록으로 구성되어 있어, 폼도 그 순서를 그대로 따라가는 게 자연스럽습니다.

| 화면 섹션 | 원문 위치 | 입력 방식 |
|---|---|---|
| ① 안내문 | 도입부(목적 설명) | 읽기 전용 텍스트 |
| ② 촬영 대상자 정보 | "1. 촬영 및 활용 대상 정보" | 텍스트 입력: 성명 / 학원명 / 연락처 |
| ③ 약관 전문 | "2. 수집 및 활용 항목" ~ "ㅇ. 기타" | 스크롤 박스 or 아코디언 (전체 노출, 접었다 펴기) |
| ④ 활용범위 및 사례금 동의 | "3. 마케팅 활용 범위 및 사례금 안내" 표 | 라디오 버튼: **동의함 / 동의하지 않음** |
| ⑤ 입금정보 | "4. 입금정보" | 텍스트 입력: 은행명 / 예금주 / 계좌번호 / 사업자등록번호 또는 주민등록번호 |
| ⑥ 최종 동의 및 서명 | 하단 확인 문구 | 체크박스("숙지하였으며 동의합니다") + **서명 캔버스** + 날짜 자동 기록 |

> 실무 팁: 약관 전문(③)은 텍스트가 길기 때문에 접었다 펼 수 있는 아코디언으로 두고, "끝까지 스크롤해야 동의 체크박스가 활성화되는" UX를 넣으면 법적 방어력이 더 좋아집니다.

---

## 3. 서명(전자서명) 구현 방안

### 방식: 캔버스 기반 드로잉 서명
- `<canvas>` 위에 마우스/터치 이벤트로 선을 그리게 하는 방식 (은행 앱, 택배 서명과 동일한 UX)
- 라이브러리: **signature_pad** (오픈소스, 가볍고 안정적) — 직접 구현보다 이 라이브러리를 쓰는 걸 권장
- "다시 서명하기" 초기화 버튼 필수

### 저장 방식
1. 서명을 그린 캔버스를 `canvas.toDataURL('image/png')`로 **Base64 이미지 문자열**로 변환
2. 제출 시 이 Base64 값을 다른 폼 데이터와 함께 전송
3. 저장 옵션 두 가지:
   - **A안 (간단)**: 스프레드시트 셀에 Base64 문자열 그대로 저장 (텍스트가 길어 시트가 무거워짐, 이미지로 안 보임)
   - **B안 (권장)**: Google Drive의 지정 폴더에 PNG 파일로 업로드 → 생성된 파일 링크만 스프레드시트에 기록 (시트에서 클릭하면 서명 이미지 확인 가능)

**B안을 권장**합니다. 시트가 가벼워지고, 담당자가 스프레드시트에서 서명 파일 링크를 클릭해 바로 확인할 수 있습니다.

### 법적 유효성 관련 참고
전자서명(그림 서명)은 실무상 널리 쓰이지만, 분쟁 시 법적 효력은 "본인이 실제로 작성했음을 증명할 수 있는가"에 좌우됩니다. 다음을 함께 기록해두면 증빙력이 올라갑니다.
- 제출 시각(타임스탬프), 제출자 IP, 접속 기기 정보를 시트에 함께 기록
- 서명 전 "본인은 위 내용을 확인하였으며 동의합니다" 체크박스를 별도로 강제

---

## 4. 구글 스프레드시트 연동 방안

가장 간단하고 안정적인 방법은 **Google Apps Script(GAS)를 웹앱으로 배포**해서 백엔드 역할을 시키는 것입니다. 서버 비용 없이 구글 계정만으로 동작합니다.

### 흐름
```
[웹페이지 폼] → (fetch POST, JSON) → [Google Apps Script 웹앱]
                                         ├─ 서명 이미지 → Google Drive에 저장
                                         └─ 나머지 데이터 → Google Sheets에 한 행 추가
```

### 개발 순서
1. 데이터를 쌓을 구글 스프레드시트 생성, 헤더 행 구성
   (예: 제출일시 / 성명 / 학원명 / 연락처 / 동의여부 / 은행명 / 예금주 / 계좌번호 / 사업자등록번호_주민번호 / 서명파일링크 / IP)
2. 확장 프로그램 → Apps Script 편집기에서 `doPost(e)` 함수 작성
   - `e.postData.contents`를 JSON.parse → 필드 추출
   - 서명 Base64 → `Utilities.base64Decode` → Blob → `DriveApp` 폴더에 저장
   - `SpreadsheetApp`으로 시트에 `appendRow([...])`
3. "배포 → 웹앱으로 배포" (액세스 권한: **모든 사용자**로 설정해야 외부 폼에서 호출 가능)
4. 발급된 웹앱 URL을 프론트엔드 폼의 제출 대상 주소로 사용

### 예시 코드 스니펫 (Apps Script, `doPost`)
```javascript
function doPost(e) {
  const data = JSON.parse(e.postData.contents);
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('제출내역');
  const folder = DriveApp.getFolderById('여기에_구글드라이브_폴더ID');

  // 서명 이미지 저장
  const sigBlob = Utilities.newBlob(
    Utilities.base64Decode(data.signature.split(',')[1]),
    'image/png',
    `${data.name}_${new Date().getTime()}.png`
  );
  const file = folder.createFile(sigBlob);
  file.setSharing(DriveApp.Access.ANYONE_WITH_LINK, DriveApp.Permission.VIEW);

  sheet.appendRow([
    new Date(), data.name, data.academyName, data.phone,
    data.consent, data.bankName, data.accountHolder, data.accountNumber,
    data.taxId, file.getUrl()
  ]);

  return ContentService.createTextOutput(JSON.stringify({ result: 'success' }))
    .setMimeType(ContentService.MimeType.JSON);
}
```

### 프론트엔드에서 호출
```javascript
fetch('https://script.google.com/macros/s/여기에_배포된_ID/exec', {
  method: 'POST',
  body: JSON.stringify(formData),
})
```
> GAS 웹앱은 CORS 응답 헤더가 제한적이라, `mode: 'no-cors'`로 보내거나 응답을 안 받는 방식으로 우회하는 경우가 많습니다. 제출 성공 여부는 "제출 중... → 제출 완료" UI로 안내하고, 실패 시 재시도 버튼을 두는 걸 권장합니다.

---

## 5. 개인정보 보호 관련 유의사항

이 폼은 **주민등록번호**를 수집할 수 있는 구조라, 일반 마케팅 동의서보다 취급에 더 신경 써야 합니다.

- 주민등록번호는 법적으로 수집이 엄격히 제한된 항목입니다. 가능하다면 **사업자등록번호로 대체 유도**하고, 부득이 주민번호를 받아야 한다면:
  - 스프레드시트에 평문으로 저장하지 말고, 별도 암호화 처리 또는 최소 접근 권한이 있는 시트로 분리
  - 해당 시트의 공유 권한을 담당자 1~2인으로 제한
- 계좌번호·주민번호 등 민감정보 필드는 화면에 "이 정보는 사례금 지급 및 세무처리 목적으로만 사용됩니다" 안내 문구 유지 (원문에도 이미 명시됨)
- 폼 페이지는 HTTPS 필수 (Netlify, Vercel, GitHub Pages 등 배포 시 기본 제공)
- 제출 완료 후 사용자에게도 확인 메일/문자를 보내고 싶다면 GAS의 `MailApp.sendEmail()`로 확장 가능 (선택 사항)

---

## 6. 기술 스택 제안 (바이브코딩 기준)

| 영역 | 추천 |
|---|---|
| 프론트엔드 | 순수 HTML + CSS + Vanilla JS (or React, 선택) — 폼 하나짜리라 복잡한 프레임워크 불필요 |
| 서명 | signature_pad.js (CDN) |
| 백엔드 | Google Apps Script 웹앱 (서버 불필요) |
| 데이터 저장 | Google Sheets (+ 서명 이미지는 Google Drive) |
| 배포 | Vercel / Netlify / GitHub Pages (무료, 정적 호스팅) |

이 조합이면 개발자 없이도 코드 몇 개 파일로 전체 흐름을 완성할 수 있고, 운영 비용도 0원입니다.

---

## 7. 다음 단계 제안

1. 실제 동의서 원문 텍스트/약관을 그대로 반영한 **HTML 폼 초안**을 바로 만들어드릴 수 있습니다.
2. 이어서 서명패드가 연동된 상태로 완성하고,
3. Google Apps Script 코드도 함께 드려서, 사용자가 본인 구글 계정에 붙여넣고 배포만 하면 되도록 안내해드릴게요.

바로 1번(HTML 폼 초안)부터 만들어드릴까요, 아니면 구글시트 헤더 구성을 먼저 같이 정하고 시작할까요?
